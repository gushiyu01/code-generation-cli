<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- Mapper XML generated by CodeGenerator -->
<mapper namespace="com.example.project.mapper.UserMapper">

    <!-- 结果集映射 -->
    <resultMap id="UserMap" type="com.example.project.entity.User">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="real_name" property="realName" />
        <result column="email" property="email" />
        <result column="phone" property="phone" />
        <result column="gender" property="gender" />
        <result column="avatar" property="avatar" />
        <result column="status" property="status" />
        <result column="dept_id" property="deptId" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 基础查询SQL -->
    <sql id="Base_Column_List">
        id, username, password, real_name, email, phone, gender, avatar, status, dept_id, create_time, update_time
    </sql>

    <!-- 查询所有 -->
    <select id="selectList" resultMap="UserMap">
        SELECT <include refid="Base_Column_List" />
        FROM sys_user
    </select>

    <!-- 根据ID查询 -->
    <select id="selectById" resultMap="UserMap">
        SELECT <include refid="Base_Column_List" />
        FROM sys_user
        WHERE id = #{id}
    </select>

    <!-- 新增记录 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (username, password, real_name, email, phone, gender, avatar, status, dept_id, create_time, update_time)
        VALUES (#{username}, #{password}, #{realName}, #{email}, #{phone}, #{gender}, #{avatar}, #{status}, #{deptId}, #{createTime}, #{updateTime})
    </insert>

    <!-- 更新记录 -->
    <update id="updateById">
        UPDATE sys_user
        <set>
            <if test="username != null">
                username = #{username},
            </if>
            <if test="password != null">
                password = #{password},
            </if>
            <if test="realName != null">
                real_name = #{realName},
            </if>
            <if test="email != null">
                email = #{email},
            </if>
            <if test="phone != null">
                phone = #{phone},
            </if>
            <if test="gender != null">
                gender = #{gender},
            </if>
            <if test="avatar != null">
                avatar = #{avatar},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="deptId != null">
                dept_id = #{deptId},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 删除记录 -->
    <delete id="deleteById">
        DELETE FROM sys_user WHERE id = #{id}
    </delete>

    <!-- 批量删除 -->
    <delete id="deleteBatchIds">
        DELETE FROM sys_user WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
