<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- Mapper XML generated by CodeGenerator -->
<!-- BaseMapper 已提供：selectList / selectById / insert / updateById / deleteById / deleteBatchIds -->
<mapper namespace="com.example.project.mapper.SysUserRoleMapper">

    <!-- 结果集映射（用于自定义复杂查询） -->
    <resultMap id="SysUserRoleMap" type="com.example.project.entity.SysUserRole">
        <id column="user_id" property="userId" />
    </resultMap>

    <!-- 基础列片段（自定义SQL中可引用） -->
    <sql id="Base_Column_List">
        user_id, role_id
    </sql>

</mapper>
