<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- Mapper XML generated by CodeGenerator -->
<!-- BaseMapper 已提供：selectList / selectById / insert / updateById / deleteById / deleteBatchIds -->
<mapper namespace="com.example.project.mapper.SysRoleOrgMapper">

    <!-- 结果集映射（用于自定义复杂查询） -->
    <resultMap id="SysRoleOrgMap" type="com.example.project.entity.SysRoleOrg">
        <id column="id" property="id" />
        <result column="org_id" property="orgId" />
        <result column="role_id" property="roleId" />
    </resultMap>

    <!-- 基础列片段（自定义SQL中可引用） -->
    <sql id="Base_Column_List">
        id, org_id, role_id
    </sql>

</mapper>
