<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- Mapper XML generated by CodeGenerator -->
<!-- BaseMapper 已提供：selectList / selectById / insert / updateById / deleteById / deleteBatchIds -->
<mapper namespace="com.example.project.mapper.SysMenuMapper">

    <!-- 结果集映射（用于自定义复杂查询） -->
    <resultMap id="SysMenuMap" type="com.example.project.entity.SysMenu">
        <id column="id" property="id" />
        <result column="code" property="code" />
        <result column="enabled" property="enabled" />
        <result column="icon" property="icon" />
        <result column="name" property="name" />
        <result column="parent_id" property="parentId" />
        <result column="path" property="path" />
        <result column="permission_code" property="permissionCode" />
        <result column="sort" property="sort" />
    </resultMap>

    <!-- 基础列片段（自定义SQL中可引用） -->
    <sql id="Base_Column_List">
        id, code, enabled, icon, name, parent_id, path, permission_code, sort
    </sql>

</mapper>
