<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- Mapper XML generated by CodeGenerator -->
<!-- BaseMapper 已提供：selectList / selectById / insert / updateById / deleteById / deleteBatchIds -->
<mapper namespace="com.example.project.mapper.SysUserMapper">

    <!-- 结果集映射（用于自定义复杂查询） -->
    <resultMap id="SysUserMap" type="com.example.project.entity.SysUser">
        <id column="id" property="id" />
        <result column="created_at" property="createdAt" />
        <result column="email" property="email" />
        <result column="enabled" property="enabled" />
        <result column="name" property="name" />
        <result column="org_id" property="orgId" />
        <result column="password_hash" property="passwordHash" />
        <result column="phone" property="phone" />
        <result column="updated_at" property="updatedAt" />
        <result column="username" property="username" />
    </resultMap>

    <!-- 基础列片段（自定义SQL中可引用） -->
    <sql id="Base_Column_List">
        id, created_at, email, enabled, name, org_id, password_hash, phone, updated_at, username
    </sql>

</mapper>
